edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
reorder_imports = true
reorder_modules = true
# imports_granularity / group_imports require nightly rustfmt; we'll
# enable them once we move to nightly fmt or stable lifts them.
